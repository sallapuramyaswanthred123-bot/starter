name: Backend CD
on:
  push:
    branches: [ main ]
jobs:
  deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./starter/backend
    steps:
      - uses: actions/checkout@v4
      - name: Build Docker image
        run: docker build -t backend .
      - name: Push to registry
        run: echo "Add your docker push commands here"